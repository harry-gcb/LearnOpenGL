if (APPLE)
    set(GL_DEPS glfw3 -framework CoreFoundation -framework CoreGraphics -framework IOKit -framework AppKit)
else()
    set(GL_DEPS glfw3 assimpd)
endif()
include(GAMES101.cmake)
include(TheCherno.cmake)
include(LearnOpenGL.cmake)
